# Placeholder; rendered at deploy time by backend/scripts/render_nginx_serve_key.py.
# Locally / pre-deploy this map degrades to header passthrough.
map $http_referer $findb_serve_proxy_key {
    default $http_x_api_key;
}
