{
  "name": "findb-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "dashboard"
  ],
  "scripts": {
    "setup": "pnpm install --frozen-lockfile && uv --directory backend sync --frozen && uv --directory backend run pre-commit install --hook-type pre-commit --hook-type pre-push",
    "dev": "pnpm dev:dashboard",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "dev:backend": "uv --directory backend run python scripts/dev.py up-server",
    "container:dashboard": "docker compose up -d --build dashboard",
    "build": "pnpm build:dashboard",
    "build:dashboard": "pnpm --filter dashboard build",
    "test": "pnpm test:dashboard && pnpm test:backend",
    "test:dashboard": "pnpm --filter dashboard test",
    "test:backend": "uv --directory backend run python scripts/dev.py test-db",
    "format": "pnpm format:dashboard && pnpm format:backend",
    "format:dashboard": "pnpm --filter dashboard format",
    "format:backend": "uv --directory backend run ruff check --fix app tests scripts migrations && uv --directory backend run black app tests scripts migrations",
    "check": "pnpm check:dashboard && pnpm check:backend && pnpm build:dashboard",
    "check:dashboard": "pnpm --filter dashboard format:check && pnpm --filter dashboard lint:check && pnpm --filter dashboard type:check && pnpm --filter dashboard test",
    "check:backend": "uv --directory backend run ruff check app tests scripts migrations && uv --directory backend run black --check app tests scripts migrations && uv --directory backend run mypy app",
    "hook:pre-commit": "uv --directory backend run pre-commit run --hook-stage pre-commit",
    "hook:pre-push": "uv --directory backend run pre-commit run --hook-stage pre-push"
  }
}
